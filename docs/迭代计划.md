# 迭代计划与优化方向（PDCA）

> 主部署：**腾讯云 VPS + Caddy HTTPS**。  
> 每轮：Plan → Do → Check → Act。本文件为真相源。

---

## 能力盘点（v0.4.0）

| 能力域 | 成熟度 |
|--------|--------|
| 腾讯云 VPS 部署 / HTTPS 反代 | ✅ 生产基线 |
| 智能路由（经 New-API） | ✅ |
| 异步提交 + worker 归档回填 | ✅ |
| 公网预签名图传 | ✅ |
| 适配器 Bearer 鉴权 | ✅ |
| 成本同步 / 看板 | ✅ |
| ComfyUI 模板内容 | ⚠️ 骨架，需专家实装 |
| 多剧隔离 | 🟡 已有 ProjectKey 字段，策略待深化 |

---

## PDCA 轮次状态

### C1 v0.3.4 — VPS 基线 + P0（已完成）
- Do：deploy/、compose 走 New-API、预签名图 URL、adapter 鉴权、cost-sync healthcheck、文档去 ZeroTier 主路径
- Check：pytest；compose config
- Act：并入 v0.4.0 发布说明

### C2 v0.4.0 — 协作闭环（已完成）
- Do：NocoDB 批量、TaskId/ErrorMsg/ShareUrl、MinIO 私有桶+归档、侧栏密钥折叠
- Check：submit-tool 单测；手册路径与代码一致
- Act：成熟度改为生产可用（ComfyUI 内容除外）

### C3 v0.5.0 — 异步与体验（已完成，随 0.4 合入）
- Do：`POST /v1/videos` 异步、`submit-worker`、ShareUrl、webhook
- Check：worker 模块 + 异步入队路径
- Act：关闭「900s 同步阻塞」主路径债

### C4 v0.6.x — 工程质量（已完成基础项，随 0.4 合入）
- Do：dashboard 边界、quota 环境变量、CI、backup.sh、ProjectKey
- Check：CI workflow；备份脚本
- Act：下季度 backlog 见下

---

## 下季度 Backlog

1. ComfyUI 真实模板（专家）
2. 多剧 IAM / MinIO 前缀策略强化
3. 夜间批量调度、结果哈希缓存
4. 多模型供应商抽象（可灵/即梦等）
5. SSO / 审计日志

---

## 版本节奏

- 补丁：热修与文档
- 次版本：闭环能力
- 主版本：架构级（新供应商等）
